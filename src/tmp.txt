#include <iostream>
#include <fstream>
#include <string>

using namespace std;

int main() {

    // ifstream f("/proc/stat");

    ifstream f("/sys/class/power_supply/BAT1/capacity");

    string line;
    getline(f, line);

    cout << line << endl;

    // getline(f, line);

    // cout << line << endl;

    return 0;
}